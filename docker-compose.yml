services:
  web:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      - DB_PATH=/app/data/db.sqlite3
    volumes:
      - ./data:/app/data
      - ./media:/app/media
    restart: unless-stopped
